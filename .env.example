# Fichier d'exemple pour les variables d'environnement.
# Copiez ce fichier dans un nouveau fichier nommé `.env.local` pour le développement.
# Le fichier `.env.local` ne doit JAMAIS être versionné (commité) sur Git.

# --- Configuration Supabase ---
# URL de l'API Gateway de votre instance Supabase locale.
# Le port par défaut exposé par le docker-compose de Supabase est 8000.
NEXT_PUBLIC_SUPABASE_URL="http://localhost:8000"

# Clé "anon" (anonyme) publique de votre instance Supabase.
# Cette clé est sécuritaire à exposer côté client (dans le navigateur).
# La valeur ci-dessous est la clé par défaut pour le développement local et n'est pas secrète.
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0"

# --- Configuration Lago ---
# URL de base de votre instance Lago pour les appels API.
# Note : Nous configurerons le service pour qu'il s'exécute sur le port 3001
# afin d'éviter tout conflit avec le serveur de développement Next.js (qui utilise le port 3000).
LAGO_API_URL="http://localhost:3001"

# --- Configuration DocuSeal ---
# URL de base de votre instance DocuSeal.
# Le port par défaut pour DocuSeal est 8080.
DOCUSEAL_URL="http://localhost:8080"

# --- Configuration Novu ---
# Clé d'API pour votre instance Novu.
# Vous trouverez cette clé dans les paramètres de votre instance Novu, sous la section "API Keys".
NOVU_API_KEY="votre-cle-api-novu"
